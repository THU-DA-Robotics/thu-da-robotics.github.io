# this file uses prebuilt image in dockerhub
services:
  jekyll:
    # image: amirpourmand/al-folio:slim
    image: crpi-az0o43zl018mzgc8.cn-beijing.personal.cr.aliyuncs.com/calaw/al-folio-irml:latest
    #build: .
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
